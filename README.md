Prophet Forecast Microservice
- POST /forecast with JSON { "dates": [...], "values": [...] }
- Returns 7-day forecast (yhat, lower, upper)

Docker-based image provided for reliable prophet installation.
